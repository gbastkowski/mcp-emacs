{
  "name": "mcp-emacs",
  "version": "0.1.0",
  "description": "MCP server for Emacs integration",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-emacs": "dist/index.js"
  },
  "files": [
    "dist",
    "elisp"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build",
    "test": "npm run build && node --test test/**/*.test.js"
  },
  "keywords": [
    "mcp",
    "emacs",
    "editor",
    "integration"
  ],
  "author": "Gunnar Bastkowski",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
